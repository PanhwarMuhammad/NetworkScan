<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_surface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- ── Identity ──────────────────────────────────────────────── -->
        <include layout="@layout/card_section_header"
            android:id="@+id/hdr_identity"
             />

        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardCornerRadius="8dp"
            app:cardElevation="2dp"
            app:cardBackgroundColor="@color/card_bg"
            android:layout_marginBottom="12dp">
            <LinearLayout android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="12dp">
                <include layout="@layout/row_label_value" android:id="@+id/row_flow_id"/>
                <include layout="@layout/row_label_value" android:id="@+id/row_timestamp"/>
                <include layout="@layout/row_label_value" android:id="@+id/row_protocol"/>
                <include layout="@layout/row_label_value" android:id="@+id/row_src_ip"/>
                <include layout="@layout/row_label_value" android:id="@+id/row_dst_ip"/>
                <include layout="@layout/row_label_value" android:id="@+id/row_src_port"/>
                <include layout="@layout/row_label_value" android:id="@+id/row_dst_port"/>
                <include layout="@layout/row_label_value" android:id="@+id/row_duration"/>

                <!-- Named TextViews (actual IDs used in fragment) -->
                <!-- We define them directly since include+id re-binding is tricky -->
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- The easiest approach: one big card with all rows directly. -->
        <!-- We use a flat list of LabelValue rows per section. -->

    </LinearLayout>

</androidx.core.widget.NestedScrollView>